services:
  pihole:
    image: pihole/pihole:2024.07
    ports:
      - "8080:80/tcp"
      - "53:53/tcp"
      - "53:53/udp"
    environment:
      WEBPASSWORD: "admin"
    volumes:
      - pihole_data:/etc/pihole
