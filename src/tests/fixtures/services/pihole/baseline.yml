services:
  pihole:
    image: pihole/pihole:2024.07
    ports:
      - "127.0.0.1:8080:80/tcp"
      - "127.0.0.1:53:53/tcp"
      - "127.0.0.1:53:53/udp"
    environment:
      WEBPASSWORD: "strong-random-password"
    volumes:
      - pihole_data:/etc/pihole
