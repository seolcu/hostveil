services:
  restic:
    image: restic/restic:latest
    container_name: restic
    restart: unless-stopped
    environment:
      RESTIC_REPOSITORY: s3:s3.amazonaws.com/mybucket
      RESTIC_PASSWORD: hardcodedpassword123
    volumes:
      - ./data:/data:ro
