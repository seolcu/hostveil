services:
  kopia:
    image: kopia/kopia:latest
    container_name: kopia
    restart: unless-stopped
    environment:
      KOPIA_PASSWORD: hardcodedpassword123
    volumes:
      - ./config:/app/config
      - ./cache:/app/cache
    ports:
      - 51515:51515
