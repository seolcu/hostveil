# Test Compose project — deliberately vulnerable, lightweight images
# Covers: ds001 (db), dr002 (web)
services:
  web:
    image: nginx:alpine
    ports:
      - "0.0.0.0:8080:80"

  db:
    image: postgres:alpine
    privileged: true

  cache:
    image: redis:alpine
