run:
  timeout: 3m

linters:
  enable:
    - errcheck
    - gosec
    - staticcheck
    - ineffassign
    - whitespace
    - misspell

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - gosec
